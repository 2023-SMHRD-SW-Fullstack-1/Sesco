<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smhrd.sesco.mapper.NoteMapper">
	
	<!-- 모든 노트 불러오기(연도별로 그룹화) -->
	<select id="selectNotesByYear" resultType="com.smhrd.sesco.domain.Note">
     SELECT *
    FROM t_note
    WHERE YEAR(n_s_date) = #{year} AND note_d_yn = 'N'
    ORDER BY n_s_date DESC
  </select>
  
  <!-- 아이 옵션 선택 했을 때 특정 아이, 연도에 따라 조회  -->
   <select id="selectNotesByKid" resultType="com.smhrd.sesco.domain.Note">
     SELECT *
 FROM t_note
 WHERE kid_seq = #{kidSeq} AND YEAR(n_s_date) = #{year} AND note_d_yn = 'N'
 ORDER BY n_s_date DESC
   </select>
</mapper>