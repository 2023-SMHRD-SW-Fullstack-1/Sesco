<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smhrd.sesco.mapper.HtSurveyMapper">

	<!-- 
	<insert id="saveResult" parameterType="java.util.Map">
		<foreach collection="socialCheckList" item="seq" separator=";">
			INSERT INTO t_survey_do (hsvd_seq) VALUES (#{seq})
		</foreach>
		<foreach collection="languageCheckList" item="seq" separator=";">
			INSERT INTO t_survey_do (hsvd_seq) VALUES (#{seq})
		</foreach>
		<foreach collection="brainCheckList" item="seq" separator=";">
			INSERT INTO t_survey_do (hsvd_seq) VALUES (#{seq})
		</foreach>
		<foreach collection="physicalCheckList" item="seq" separator=";">
			INSERT INTO t_survey_do (hsvd_seq) VALUES (#{seq})
		</foreach>
	</insert>
	 -->
	 
	 <insert id="saveResult" parameterType="com.smhrd.sesco.domain.SurveyDo">
		insert into t_survey_do (hsvd_seq)
		values(#{seq})
	</insert>

</mapper>